



<%= render 'devise/shared/script' %>
  <div id="copy"><a href="<%=movies_path %>"><h3 class ="heading"><b>MovieStore</b></h3></a></div>
    <div class="signupmask">
      <div class="signupmodal  display">
        <div class="login">
          <h1 class="signupheading">Edit Account</h1>
            <div class="form">
              <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
                <form method="post">
                  <%= devise_error_messages! %>
                  <div class="form-inputs">
                  <!-- <div clas="row"> -->
                    <!-- <div class="col-sm-4"> -->
                      <div class="form-group userimage">
                        <%= f.label :name %>
                        <%= f.text_field :name, class: "form-control", :autofocus => true %>
                      </div>
                      <div class="form-group">
                        <%= f.label :Profile_Picture %>
                        <%= f.file_field :photo, class: "form-control" %>
                      </div>
                      <div class="form-group">
                        <%= f.label :email %>
                        <%= f.email_field :email, class: "form-control", :autofocus => true %>
                      </div>
                    <!-- </div> -->
                    <!-- <div class="col-sm-4">
                      <%= image_tag  current_user.photo, class: "rating-show-image" %>
                    </div> -->
                  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
                  <% end %>

                  <div class="form-group">
                    <%= f.label :new_password %>
                    <%= f.password_field :password, class: "form-control", :autocomplete => "off" ,placeholder: "write if you want to change it(6 characters minimum)"%>
                  </div>

                  <div class="form-group">
                    <%= f.label :current_password %>
                    <%= f.password_field :current_password, class: "form-control" %>
                  </div>
                  </div>
                  <div class="form-group signupform">
                    <%= f.submit "Update", class: "btn btn-primary signupbtn" %>
                  </div>

              </div>
              <% end %>
              </form>
            </div>
          <br>
            <% #render "devise/shared/links" %>
        </div>
      </div>
     </div>



<%= render 'devise/shared/signlogback' %>
